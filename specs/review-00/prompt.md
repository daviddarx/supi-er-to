1. Create a favicon with both letters SU with the same font as the title, bold. white on black.
2. The website gutter is good as it is. Centralize it in a global css variable, in the tailwind config. On mobile, the header is correctly aligned with this gutter, but as soon as the header get fixed, it's gutter is too small. Use the centralized variable their too.
3. Header: on mobile, place the select on it's own row, full width. Below it, the view options buttons and the the dark-mode button should be in a flex with space between, the view options left aligned, the dark-mode mode button right-aligned.
4. Loaded images: the grey background is displayed above the loaded images, it should be below.
5. General: remove all the 2px border radius. Also the border radius in the tooltips.
6. General: for the grey lines (i.e buttons border) allways use white with opacity (dark-mode) and black with opacity (light mode), instead of plain colors.
7. When the lightmode is selected and saved in the local storage, on page load, the background is first dark before it goes white. Move the logic in the head as inline js, to avoid any delay in the darkmode attribution and avoid layout shift.
8. Use 14px text, instead of 12px now.
9. Use scrollbar-gutter: stable; on all container which are supposed to have a scroll, to avoid layout shift when a scrollbar appears. Including body. The problem to solve is now: when I open the select (bone/supi), shadcn deactivate the scroll on the page, end the right part of the header jumps to the right, as there isn't any scrollbar anymore. Test exactly if this problem is solved.
10. Style the scrollbars according the example in the senior-coder agent.
11. Header options buttons: group new arrival and log out buttons, same as the view options buttons are groupped.
12. Header options buttons: buttons have now a 32px size. Increase padding so that they have 40px size.
13. Header options buttons: in dark-mode, the buttons have a lighter border than the select. should look like the select.
14. Header view options buttons: refactor the following icons:
- Classic: the 3 boxes should have the same height and width, below each other.
- Explorative: no rotation for the boxes, and a slight overlapp. 
15. Header options buttons: the icons strokes don't look like 1px. They are dickier, and blurry. Refactor the icons to have perfect 1px line. Also place them correctly in the svg to avoid blurry rending on half pixells. Update this also for all other icons (prev, next, close), they all look blurry and dickier.
16. Header options buttons: when buttons are active (plain black in lightmode for example) they still have the grey border, which make them feel blurry. Solution: don't put the border on the container, but directly on the buttons (the buttons in the middlle will only have a top, bottom, and right border), so that you can swhich this color to the same color has the background on hover. Give a common style to all buttons, and manage the differences for the fist and the last one with css selectors for first and last child, so that I can easily add or  remove a button and update the position of the buttons.
17. Header options buttons: in the dark-mode, the hover state of the groupped buttons doesn't looks like the hover-state of the single buttons. The border of the single buttons stays visible on hover, but is not visible anymore for the grouped icons. it should be visible. Also, the select has a light bbackground color. It should look transparent, like the other buttons.
18. Select in the options (bone/supi): several problems: when closed, the text ist cutted on the right. When opened, the text is bigger than when closed, which cause a layout shift. The font-size should be 14px for both. And the goal is that the text stays exactly at the same place, when open or closed, to avoid layout shift. Other problem: the selec doesn't have the same height has the other buttons in header. It's primordial that they have the same height, update paddings and line-height to achieve that.
19. Header: you could actually switch the fixed position with a sticky top, it will achieve the same and then you don't have to calculate the free space below the header, it will have a natural flow.
20. Classic Mode: on desktop, the gap between pictures should be 15vh. The spacing between header and first picture should be also 15vh.
21. Grid Mode: the gap between pictures should be 1px.
22. Grid Mode: on mobile too, the grid should have at least 2 columns, if else there is no. difference with the classic mode.
23. Grid Mode: on desktop, do not limit the width of the grid, let it take 100% of the width, until the website gutter.
24. Explorative mode: the images positioning is good for all images. When I choose a subset though (bone or supi) the positioning isnt calculated again anymore, so there is a lot of "holes" in the composition, where the hidden images was. Recalculate position to have same effect as with the full set of picture.
25. Explorative mode: remove the icon on hover of the images. When an image is hovered, place it above the other images, changes the z-index. And reinit the rotation to 0, with animation. on mouseout, add the same rotation again. Make the full image clickable (instead of only the icon currently) to open the fullscreen carousel mode. Important, check that the behavior doens't interfer with the drag and drop. The carousel should only be launched if the user click (mousedown and mouseup very shortly after that, on the same position) the image, also on touch screens.
26. Fullscreen carousel mode: also add a fade in there for the images when they are loaded.
27. Fullscreen carousel mode: the buttons aren't styled the same as the header options buttons. They should. Ideally, use the same component, with same hover, etc.
28. Fullscreen carousel mode: nothing happens when I click on prev and next buttons.
29. Fullscreen carousel mode: when I click the close button, it suddenly remove the fullscreen overlay, without animation out. It should have the same animation out as when I click on the background overlay, or when I hit the esc key.
30. Zoom icon: for the classic, grid and explorative mode, I wand you to create a centralized system to show zoom icon on hover, giving the user a hint of the carrousel fullscreen overlay. Create, outside of the views (at the same level as header and views container), an icon that you'll animate on hover of the images. The icon has intially opacity 0 and is positionned in the middle of the screen. The icon should be a + in a circle (white icon on black background in dark mode, inverted in lightmode). When the user hovers a image, fade the icon in and  animate the position of the icon  to follow the mouse of the user (mouse pos x/y + 20 in both axes, and important: with a slight inertia/ease), as long as he hovers an image. On mouse out of an image, stop the positioning update and fade the icon out. If the user mouse out an image and directly mouse hover an other image, he shoud have the feeling that there was no interuption between both images. Means: add a small delay before you fade the icon out on mouse out, and stop positioning.
31. Please add the view name as a route too. I.e /classic, /grid, /experimental, etc. And a subroute for the images opened in the carrousel mode. I.e /classic/24. Do not forget co clean up well and remove the code for the current routes, which are working with search params, i.e /?image=supi-29
32. By the way. I am not that happy with the id of opened images. For now, for the basic set, you use the tag and id (ie. supi-29, bone-81). And for the newly uploaded images, you use img-{long-id}, ie. img-1773140516009. Please keep the system with tag and short id, also for newly added images. Also for photos images for example, currently photos-27. Just use the tag (supi or bone) and a short id for all images.
33. Are you sure that you used the instrensic sizes of the images to add them to the img tags to avoid layout shift when the images are loaded? I still get some layout shift, in the grid mode for example. This would mean that you should, by upload of the images, save their intrasic size in the @public/data/images.json, in order to serve it before images are loaded.
34. The admin sheet to add new images hasn't any padding for it's content. Please add padding to align with the sheet header. 
35. Add PWA capabilities through webmanifest so allow app to be fullscreen on iOS when added to start screen. What other PWA features could be interesting for me? 